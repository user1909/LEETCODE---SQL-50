# Write your MySQL query statement below

SELECT DISTINCT author_id AS id
FROM Views
WHERE viewer_id = author_id
ORDER BY id;

Since authors that viewed at least one of their own articles therefore  viewer_id and  author_id must be equl or same.
